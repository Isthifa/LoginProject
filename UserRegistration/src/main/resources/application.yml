spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/userdb
    username: postgres
    password: root
  jpa:
      hibernate:
          ddl-auto: update
      show-sql: true

logging:
  level:
    sql: trace
